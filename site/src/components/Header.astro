---
import { links } from "../data/links";
---

<header class="site-header">
  <nav class="nav shell" aria-label="Main navigation">
    <a class="brand" href="/" aria-label="Pastey home">
      <img src="/favicon.png" alt="" width="30" height="30" />
      <span>Pastey</span>
    </a>
    <div class="nav-links" id="nav-links">
      <a href="/#features">Features</a>
      <a href="/#how-it-works">How it works</a>
      <a href="/architecture">Architecture</a>
      <a href="/download">Download</a>
      <a href={links.github}>GitHub</a>
    </div>
    <a class="button button-small button-quiet desktop-download" href={links.latestRelease}>
      <i class="ph ph-download-simple" aria-hidden="true"></i>
      Download latest
    </a>
    <button class="menu-button" aria-expanded="false" aria-controls="nav-links" aria-label="Toggle navigation">
      <i class="ph ph-list" aria-hidden="true"></i>
    </button>
  </nav>
</header>

<script>
  const menuButton = document.querySelector<HTMLButtonElement>(".menu-button");
  const navLinks = document.querySelector<HTMLElement>(".nav-links");
  menuButton?.addEventListener("click", () => {
    const expanded = menuButton.getAttribute("aria-expanded") === "true";
    menuButton.setAttribute("aria-expanded", String(!expanded));
    navLinks?.classList.toggle("is-open", !expanded);
  });
  navLinks?.querySelectorAll("a").forEach((link) => link.addEventListener("click", () => {
    menuButton?.setAttribute("aria-expanded", "false");
    navLinks?.classList.remove("is-open");
  }));
</script>
