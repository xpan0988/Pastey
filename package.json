{
  "name": "pastey",
  "private": true,
  "version": "1.9.0",
  "author": "xpan0988",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "tauri:dev": "npm run tauri -- dev",
    "tauri:dev-fast": "npm run tauri -- dev -- --profile dev-fast",
    "build": "tsc && vite build",
    "tauri:build": "npm run tauri -- build",
    "check:version": "node scripts/check-version-consistency.mjs",
    "release:version": "node scripts/release-version.mjs",
    "build:checked": "npm run check:version && npm run tauri:build && node scripts/check-build-size.mjs",
    "preview": "vite preview",
    "tauri": "cross-env CI=true tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "cross-env": "^7.0.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
